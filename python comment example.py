# say or # space say allows for comment to be written about the code in order ot rember just what was written. for example, str allows stringing of numbers into the code . the #say function ensures the first line of code is ignored and the following code is run 
age = 27
message = "Happy " + str(age) + "rd Birthday"
print (message) 
